Stockfish engine assets are expected in this directory for the standalone/offline build.
The application loads stockfish-18-lite-single.js from this local directory so the Worker is not dependent on a CDN.
